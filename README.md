# gpu-telemetry-dashboard
Live GPU fleet telemetry dashboard simulating thermal/power behavior on H100/A100 nodes, with two-layer anomaly detection (threshold + rate-of-change) and a Flask API + real-time dashboard — built to demonstrate the diagnostic logic behind keeping dense GPU infrastructure running.
